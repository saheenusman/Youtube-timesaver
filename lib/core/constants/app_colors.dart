import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF000000);
  static const surface = Color(0xFF111111);
  static const surfaceElevated = Color(0xFF1A1A1A);

  // Primary (YouTube Red)
  static const primary = Color(0xFFFF0000);
  static const primaryDim = Color(0xFFCC0000);

  // Whites
  static const white = Color(0xFFFFFFFF);
  static const whiteTransparent = Color(0x4DFFFFFF);
  static const whiteDim = Color(0xFFB3B3B3);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const textTertiary = Color(0xFF777777);

  // Status
  static const success = Color(0xFF00FF00);
  static const accent = Color(0xFFFF0000);

  // Agent Colors
  static const agentTeacher = Color(0xFF00FF00);
  static const agentAnalyst = Color(0xFF00BFFF);
  static const agentExplorer = Color(0xFFFF69B4);
}
